<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId> com.github.cuioss</groupId>
    <artifactId>cuioss-parent-pom</artifactId>
    <packaging>pom</packaging>
    <version>1.0-SNAPSHOT</version>
    <description>Parent pom for all cui-oss projects. It defines the sonatype repositories and the basic maven plugins: 
    maven-clean-plugin, maven-deploy-plugin, maven-enforcer-plugin, maven-install-plugin with being already activated.</description>
    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- Repository-Urls -->
        <public.repo.url>https://oss.sonatype.org/content/groups/public</public.repo.url>
        <staging.repo.url>https://oss.sonatype.org/content/groups/staging</staging.repo.url>
        <deploy.snapshot.repo.url>https://oss.sonatype.org/content/repositories/snapshots</deploy.snapshot.repo.url>
        <deploy.staging.repo.url>https://oss.sonatype.org/service/local/staging/deploy/maven2</deploy.staging.repo.url>
        <!-- Standard maven plugins: org.apache.maven. -->
        <maven.clean.plugin.version>3.1.0</maven.clean.plugin.version>
        <maven.deploy.plugin.version>2.8.2</maven.deploy.plugin.version>
        <maven.enforcer.plugin.version>1.4.1</maven.enforcer.plugin.version>
        <maven.install.plugin.version>2.5.2</maven.install.plugin.version>
        <maven.site.plugin.version>3.7.1</maven.site.plugin.version>
        <!-- org.codehaus plugins -->
        <enforcer.extra.rules.version>1.0-beta-6</enforcer.extra.rules.version>
    </properties>
    <inceptionYear>2018</inceptionYear>
    <url>https://github.com/cuioss/oss-parent-pom/</url>
    <scm>
        <url>https://github.com/cuioss/oss-parent-pom/</url>
        <connection>https://github.com/cuioss/oss-parent-pom.git</connection>
    </scm>
    <issueManagement>
        <url>https://github.com/cuioss/oss-parent-pom/issues</url>
        <system>GitHub Issues</system>
    </issueManagement>
    <licenses>
        <license>
            <name>The Apache Software License, Version 2.0</name>
            <url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>
    <contributors>
        <contributor>
            <name>Oliver Wolff</name>
            <url>https://github.com/cuioss</url>
            <roles>
                <role>owner</role>
                <role>developer</role>
            </roles>
        </contributor>
    </contributors>
    <organization>
        <url>https://icw-global.com/de/</url>
        <name>InterComponent Ware</name>
    </organization>
    <repositories>
        <repository>
            <id>sonatype-nexus-snapshots</id>
            <name>Sonatype Nexus Snapshots</name>
            <url>${snapshot.repo.url}</url>
            <releases>
                <enabled>false</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>sonatype-nexus-public</id>
            <name>Sonatype Nexus Public Repo</name>
            <url>${public.repo.url}</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
    </repositories>
    <distributionManagement>
        <snapshotRepository>
            <id>sonatype-nexus-snapshots</id>
            <name>Sonatype Nexus Snapshots</name>
            <url>${deploy.snapshot.repo.url}</url>
        </snapshotRepository>
        <repository>
            <id>sonatype-nexus-staging</id>
            <name>Nexus Release Repository</name>
            <url>${deploy.staging.repo.url}</url>
        </repository>
    </distributionManagement>
    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <artifactId>maven-clean-plugin</artifactId>
                    <version>${maven.clean.plugin.version}</version>
                </plugin>
                <plugin>
                    <artifactId>maven-deploy-plugin</artifactId>
                    <version>${maven.deploy.plugin.version}</version>
                </plugin>
                <plugin>
                    <artifactId>maven-install-plugin</artifactId>
                    <version>${maven.install.plugin.version}</version>
                </plugin>
                <plugin>
                    <artifactId>maven-site-plugin</artifactId>
                    <version>${maven.site.plugin.version}</version>
                </plugin>
                <plugin>
                    <artifactId>maven-enforcer-plugin</artifactId>
                    <version>${maven.enforcer.plugin.version}</version>
                    <executions>
                        <execution>
                            <id>mojo-enforcer-rules</id>
                            <phase>validate</phase>
                            <goals>
                                <goal>enforce</goal>
                            </goals>
                            <configuration>
                                <rules>
                                    <requirePluginVersions>
                                        <message>Best Practice is to always define plugin versions!</message>
                                        <banLatest>true</banLatest>
                                        <banRelease>true</banRelease>
                                    </requirePluginVersions>
                                    <requireMavenVersion>
                                        <version>[3.0.5,)</version>
                                        <message>Maven 3.0.5 is the minimal requirement.</message>
                                    </requireMavenVersion>
                                    <requireJavaVersion>
                                        <version>[1.8.0-73,1.9.0)</version>
                                        <message>Invalid Java Version. Make sure you have the latest patch of Java8.
                                            Current version: ${java.version}</message>
                                    </requireJavaVersion>
                                    <requirePropertyDiverges>
                                        <message>Children must have a diverging name</message>
                                        <property>project.name</property>
                                    </requirePropertyDiverges>
                                    <requirePropertyDiverges>
                                        <message> Children must have a diverging description</message>
                                        <property>project.description</property>
                                    </requirePropertyDiverges>
                                    <requireSameVersions>
                                        <plugins>
                                            <plugin>org.apache.maven.plugins</plugin>
                                        </plugins>
                                    </requireSameVersions>
                                </rules>
                            </configuration>
                        </execution>
                    </executions>
                    <dependencies>
                        <dependency>
                            <groupId>org.codehaus.mojo</groupId>
                            <artifactId>extra-enforcer-rules</artifactId>
                            <version>${enforcer.extra.rules.version}</version>
                        </dependency>
                    </dependencies>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <artifactId>maven-enforcer-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>